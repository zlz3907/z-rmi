##
project.name = ${ant.project.name}
package.type=jar
target=1.7
source=1.7
web.root=WebContent

##
#src.dir=src:gen-java
src.dir=src
src.test=test
lib.dir=lib

##
build.dir=build
build.src.dir=${build.dir}/main
build.test.dir=${build.dir}/test
dist.dir=dist
dist.lib=${dist.dir}/${project.name}

## maven
pom.project.name=${project.name}
pom.project.version=${version}

resolve.shared.local.path = file:///${basedir}/ivysettings-gitlab.xml
resolve.shared.settings.file = ${resolve.shared.local.path}